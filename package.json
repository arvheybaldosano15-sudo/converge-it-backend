{
  "name": "converge-it-ticketing-system",
  "version": "1.0.0",
  "description": "Mobile-Based Messenger Integrated Customer Support and Automated Ticketing System for Converge IT Solutions",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"SERVER,ADMIN,TECH\" -c \"cyan,green,yellow\" --kill-others-on-fail \"npm run dev:server\" \"npm run dev:admin\" \"npm run dev:tech\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev:admin": "cd client && npm run dev:admin",
    "dev:tech": "cd client && npm run dev:tech",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
